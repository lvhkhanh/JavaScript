function spend(total, amount) {
  // TODO: check amount should be less than total
  return total > amount;
}

function add(total, amount) {
  // TODO: add amount savings to total
  total += amount;
}
